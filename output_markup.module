<?php

/**
 * Implements hook_theme().
 */
function output_markup_theme($existing, $type, $theme, $path) {
  return array(
    'markup_block' =>
           array(
            'variables' => array(
             'title' => 'Default title',
            ),
        )
    );
}
